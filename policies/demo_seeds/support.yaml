policies:
  - name: deny_bulk_account_lookup
    description: "Block wildcard account_id in customer account queries — support demo"
    principal_type: null
    principal_id: null
    action_tool: read_customer_account
    resource_system: null
    effect: deny
    condition:
      parameter_match:
        account_id: "*"
    active: true
    severity: critical
    compliance_frameworks: ["GDPR", "SOC2", "CCPA"]
