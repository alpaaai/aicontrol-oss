[pytest]
asyncio_mode = auto
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    live_llm: hits the real LLM provider; requires a key; excluded from the default run
addopts = -m "not live_llm"

